# 环境检测报告

**检测日期**: 2026-03-21
**系统**: Linux (WSL2) - 6.6.87.2-microsoft-standard-WSL2
**Shell**: /bin/bash

---

## 1. Claude Code 本体

| 项目 | 值 |
|------|-----|
| 版本 | 2.1.76 |
| 安装方式 | mise (`~/.config/mise/config.toml`) |
| 二进制路径 | `~/.local/share/mise/installs/npm-anthropic-ai-claude-code/2.1.76/bin` |

> **注意**: 审计报告中记录版本为 2.1.80，当前检测为 2.1.76，可能存在版本回退或两份安装并存。

---

## 2. 运行时环境

| 工具 | 版本 | 安装方式 |
|------|------|----------|
| Node.js | v24.13.1 | 系统 |
| npm | 11.10.0 | 系统 |
| npx | 11.10.0 | 系统 |
| bun | 1.3.9 | `~/.bun/bin` |
| Python | 3.13.9 | 系统 |
| git | 2.45.2 | 系统 |
| npm global prefix | `~/.npm-global` | - |

---

## 3. mise 管理的工具

| 工具 | 版本 |
|------|------|
| bat | 0.26.1 |
| delta | 0.18.2 |
| eza | 0.23.4 |
| fd | 10.4.1 |
| fzf | 0.70.0 |
| jq | 1.8.1 |
| lazygit | 0.59.0 |
| ripgrep | 15.1.0 |
| yq | 4.52.4 |
| @anthropic-ai/claude-code | 2.1.76 |

---

## 4. ~/.claude/settings.json

```json
{
  "env": {
    "ANTHROPIC_AUTH_TOKEN": "sk-ant-oat01-...(已脱敏)",
    "ANTHROPIC_BASE_URL": "https://code.newcli.com/claude/ultra",
    "CLAUDE_CODE_DISABLE_NONESSENTIAL_TRAFFIC": 1
  },
  "permissions": {
    "allow": [],
    "deny": []
  }
}
```

**状态**:
- 无 `hooks` 字段（hooks 未配置到全局 settings.json）
- 无 `mcpServers` 字段（MCP 配置在 `~/.mcp.json`）
- 使用自定义 `ANTHROPIC_BASE_URL`（非官方端点）

---

## 5. MCP 服务器配置 (~/.mcp.json)

| 服务器 | 包 | 状态 |
|--------|-----|------|
| context7 | `@upstash/context7-mcp` | 已配置，通过 npx 按需运行 |
| exa | `exa-mcp-server` | 已配置，EXA_API_KEY 已设置 |
| github | `@modelcontextprotocol/server-github` | 已配置，GITHUB_TOKEN 已设置 |

当前会话 MCP 激活状态（来自 `.claude/settings.local.json`）: context7, exa, github 均已启用

---

## 6. Rules

路径: `~/.claude/rules/common/`，共 8 个文件

| 文件 | 内容 |
|------|------|
| agents.md | Agent 编排规则 |
| coding-style.md | 编码风格规则 |
| git-workflow.md | Git 工作流规则 |
| hooks.md | Hooks 系统说明 |
| patterns.md | 通用设计模式 |
| performance.md | 性能优化策略 |
| security.md | 安全规范 |
| testing.md | 测试要求 |

---

## 7. Hooks 配置

**全局 `~/.claude/settings.json`**: 无 hooks 字段（未配置）

**项目模板 `hooks/hooks.json`** 中定义了以下 hooks（参考配置，未实际安装）:

| 类型 | 触发条件 | 功能 |
|------|----------|------|
| PreToolUse | Bash | 阻止在 tmux 外运行 dev server |
| PreToolUse | Bash | 建议长命令在 tmux 中运行 |
| PreToolUse | Bash | git push 前提醒 review |
| PreToolUse | Write | 阻止创建不必要的 .md 文件 |
| PreToolUse | Edit\|Write | 建议 compact 操作 |
| PreCompact | * | 压缩前保存状态 |
| SessionStart | * | 加载上一次会话上下文 |
| PostToolUse | Bash | PR 创建后记录 URL |
| PostToolUse | Bash | 构建完成后后台分析 |
| PostToolUse | Edit | Prettier 格式化 JS/TS |
| PostToolUse | Edit | TypeScript 类型检查 |
| PostToolUse | Edit | 警告 console.log |
| Stop | * | 检查 console.log |
| SessionEnd | * | 持久化会话状态 |

> **注意**: hooks.json 中的 `${CLAUDE_PLUGIN_ROOT}/scripts/hooks/*.js` 依赖插件脚本，这些脚本不在当前环境中，相关 hooks 若直接安装会失败。

---

## 8. 插件系统

### Marketplace 状态

| Marketplace | 来源 | 最后更新 | 状态 |
|-------------|------|----------|------|
| claude-plugins-official | `anthropics/claude-plugins-official` | 2026-03-20 | 已缓存 |

### 可用官方插件（缓存中，未激活）

**功能类**:
agent-sdk-dev, claude-code-setup, claude-md-management, code-review, code-simplifier, commit-commands, example-plugin, explanatory-output-style, feature-dev, frontend-design, hookify, learning-output-style, playground, plugin-dev, pr-review-toolkit, ralph-loop, security-guidance, skill-creator

**LSP 类**:
clangd, csharp, gopls, jdtls, kotlin, lua, php, pyright, ruby, rust-analyzer, swift, typescript

**External 类（外部集成目录存在，未安装）**:
asana, context7, discord, fakechat, firebase, github, gitlab, greptile, laravel-boost, linear, playwright, serena, slack, stripe, supabase, telegram

### Blocklist

| 插件 | 原因 |
|------|------|
| code-review@claude-plugins-official | just-a-test |
| fizz@testmkt-marketplace | security |

---

## 9. 全局 CLAUDE.md

| 路径 | 状态 |
|------|------|
| `~/CLAUDE.md` | **不存在** |

---

## 10. ~/.claude/ 目录结构

```
~/.claude/
├── backups/
├── cache/
├── debug/
├── downloads/
├── file-history/
├── history.jsonl
├── plans/
├── plugins/
│   ├── blocklist.json
│   ├── known_marketplaces.json
│   └── marketplaces/
│       └── claude-plugins-official/
├── projects/
├── rules/
│   └── common/ (8个规则文件)
├── session-env/
├── sessions/
├── settings.json
└── shell-snapshots/
```

> **注意**: 无 `agents/` 目录（自定义 agents 未配置）

---

## 11. Bundled Skills（内置，无需安装）

| Skill | 触发场景 |
|-------|----------|
| simplify | 代码审查和简化 |
| loop | 定时循环任务 (`/loop`) |
| claude-api | 使用 Anthropic SDK/API 开发 |

---

## 12. 与上次审计的差异

| 项目 | 上次审计 (2026-03-21 初始) | 本次检测 |
|------|--------------------------|---------|
| Claude Code 版本 | 2.1.80 | 2.1.76 |
| Rules | 待安装 | **已安装** (8个文件) |
| MCP 配置 | 已存在 | 已存在（无变化） |
| hooks | 未配置 | **仍未配置**到 settings.json |
| ~/CLAUDE.md | 不存在 | **仍不存在** |
| agents/ 目录 | 未提及 | 不存在 |

---

## 13. 待办建议

| 优先级 | 项目 | 说明 |
|--------|------|------|
| 高 | 确认 Claude Code 版本 | 审计记录 2.1.80，当前为 2.1.76，需核查 |
| 中 | 配置 Hooks | 将简单 hooks（不依赖插件脚本的）合并到 settings.json |
| 中 | 创建 ~/CLAUDE.md | 从项目模板 CLAUDE.md 复制 |
| 低 | 按需安装插件 | commit-commands, pr-review-toolkit 等实用插件 |
| 低 | 配置自定义 agents | 创建 ~/.claude/agents/ 并参考 agents.md 配置 |
